/**
 * Placeholder for the quotation list. Rebuilt against the real contract in a
 * later step of this phase; here only so the route renders and the theme can
 * be checked end to end.
 */
export default function QuotationsPage() {
  return (
    <div className="flex flex-col gap-1">
      <h1 className="text-2xl font-bold leading-8 text-ink">Quotations</h1>
      <p className="text-sm text-muted">The list and builder arrive in the next steps.</p>
    </div>
  )
}
